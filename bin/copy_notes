#!/usr/bin/env bash

set -e

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )/.." && pwd )"
NOTES_DIR=/alex/notes

yes | cp -rf $NOTES_DIR/movies.ft $DIR
yes | cp -rf $NOTES_DIR/movies $DIR
yes | cp -rf $NOTES_DIR/everyday.ft $DIR